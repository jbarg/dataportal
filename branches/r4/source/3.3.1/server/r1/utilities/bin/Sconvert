#!/bin/bash
 
# require at least two arguments
if [ $# -lt 2 ]; then
  exit
fi
 
 
# parse arguments
i=1
Z=$(($# - 1))
while [ $i -lt $Z ]; do
  i=$(($i + 1))
  args="$args $1"
  shift
done
origfile=$1
newfile=$2

Spcommand -d $origfile "convert $args $newfile" > $newfile
